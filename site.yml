---
- hosts: all
  sudo: yes
  gather_facts: true # roles use ansible_os_family
  vars:
      java_packages: openjdk-7-jdk
  vars_files:
    - vars/main.yml  
  roles:
    - { role: tomcat6 }
    - { role: postgresql }
    - { role: xnat }
      

